# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Vector SDK config and secrets (never commit robot credentials)
.anki_vector/
*sdk_config.ini
*.cert
*.pem
.env

# Pi boot-partition staging files that carry secrets (password hash, Wi-Fi).
# Only the sanitized templates (*.example) are tracked.
infra/raspberry-pi/boot-files/userconf.txt

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/
